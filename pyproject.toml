[project]
name = "inference-xray"
version = "0.1.0"
description = "Educational platform for visualizing LLM inference."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "gradio>=6.20.0",
]
